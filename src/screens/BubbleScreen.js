import React from "react";
import { Text, StyleSheet } from "react-native";

const BubbleScreen = () => {
  return <Text style={styles.text}>Bubble</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default BubbleScreen;
